% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scgas_compute.R
\name{scgas_add_assay}
\alias{scgas_add_assay}
\title{Add scGAS Matrix as an Assay Inside a SeuratObject}
\usage{
scgas_add_assay(
  seurat_obj,
  scgas_mat,
  run_dim_reduction = TRUE,
  n_pcs = 30L,
  n_neighbors = 30L,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{\code{SeuratObject}.}

\item{scgas_mat}{Genes x cells numeric matrix as returned by
\code{\link{scgas_compute}}.}

\item{run_dim_reduction}{Logical.  If \code{TRUE} (default) run
\code{FindVariableFeatures}, \code{ScaleData}, \code{RunPCA}, and
\code{RunUMAP} on the new assay.}

\item{n_pcs}{Integer.  Number of PCs for PCA and UMAP.  Default \code{30}.}

\item{n_neighbors}{Integer.  \code{n.neighbors} passed to
\code{RunUMAP}.  Default \code{30}.}

\item{verbose}{Logical.  Default \code{TRUE}.}
}
\value{
The \code{SeuratObject} with a new \code{scGAS} assay (and
  optionally a \code{scgasumap} reduction).
}
\description{
Convenience wrapper that stores the genes x cells scGAS matrix as a new
\code{"scGAS"} assay and, optionally, re-runs PCA and UMAP on the scGAS
values to produce a cell embedding.
}
\examples{
\dontrun{
obj <- scgas_add_assay(obj, scgas_mat)
}

}
